import { MD3LightTheme } from 'react-native-paper';

/**
 * Default Material 3 theme for now — a real palette/dark-mode pass is scoped to ROADMAP.md
 * "Фаза 8" (Полировка), once there's enough UI built to design against.
 */
export const paperTheme = MD3LightTheme;
